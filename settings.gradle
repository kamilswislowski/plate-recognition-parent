pluginManagement {
    repositories {
        maven {
            url 'maven-repo'
        }
        gradlePluginPortal()
        ivy {
            url 'ivy-repo'
        }
    }
}
rootProject.name = 'plate-recognition-spring'

