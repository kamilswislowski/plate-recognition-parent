pluginManagement {
    repositories {
        maven {
            url 'maven-repo'
        }
        gradlePluginPortal()
        ivy {
            url 'ivy-repo'
        }
    }
}
rootProject.name = 'plate-recognition-parent'
include 'plate-recognition-model'
include 'plate-recognition-spring'

