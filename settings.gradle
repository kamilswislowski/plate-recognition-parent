pluginManagement {
    repositories {
        maven {
            url 'maven-repo'
        }
        gradlePluginPortal()
        ivy {
            url 'ivy-repo'
        }
    }
}
rootProject.name = 'plate-recognition-spring'
include 'plate-recognition-model'
include 'plate-recognition-spring-x'

